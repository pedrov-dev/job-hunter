# JobBot dependencies
# pip install -r requirements.txt

openai>=1.30
playwright>=1.44
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2          # XML parser for RSS feeds
markdown>=3.6
weasyprint>=62.0   # PDF generation (also needs system libs — see docs)
python-dotenv>=1.0
